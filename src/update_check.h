/* Compare the current version to the latest version on server.
   This passes the current version number as *version_number 
   and returns the string of the latest version by querying the
   server. */

char *update_check(char *version_number);
